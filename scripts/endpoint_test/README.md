### Endpoint Test

run the test - in this directory

```
node testendpoints.js
```

input endpoints are in endpoints.csv

output is stored in endpoint_rankings.csv

if the script hangs at a specific node, please abort it and commeent out that node in the list with #

I haven't gotten around to adding hard timeouts so it will just hang for a very long time.